# Dependency regression challenge

On a disposable branch, pin an intentionally vulnerable version of one direct dependency. Verify that the Trivy filesystem/container scan detects it. Restore the secure version before merge.
